# 20260418-留学生管理系统业务模块命名重构任务清单

## 需求信息

| 字段 | 内容 |
| --- | --- |
| 文档日期 | 2026-04-18 |
| 需求名称 | 留学生管理系统业务模块命名重构 |
| 项目 | `nest-demo` |
| 需求来源 | 用户确认需要将业务模块按项目语义重命名，不继续以 `biz-content` 作为对外主模块名 |
| 交付目标 | 建立“留学生管理系统”自己的业务模块入口命名，并保留现有接口兼容能力 |
| 负责人 | Codex |

## 工作清单与完成情况

| 序号 | 工作项 | 完成情况 | 影响范围 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | 新建留学生业务模块目录 | 已完成 | `src/student-business/` | 建立新的项目语义化模块入口 |
| 2 | 新建控制器、服务、常量、DTO 文件 | 已完成 | `src/student-business/*` | 文件命名不再照抄 Bird 或使用 `biz-content` 作为主入口 |
| 3 | 保留 `/biz/content` 接口兼容 | 已完成 | `src/student-business/student-business.controller.ts` | 前端现有调用不需要立即改造 |
| 4 | 切换主模块注册入口 | 已完成 | `src/app.module.ts` | 应用主模块改为引入 `StudentBusinessModule` |

## 功能范围与边界

| 类别 | 本次范围 | 边界说明 |
| --- | --- | --- |
| 模块命名 | 对外主业务模块命名重构 | 已完成 |
| 接口路径 | 继续保留 `/biz/content` | 本次不强制改前端接口地址 |
| 深层拆分 | 仍复用现有分表映射逻辑 | 下一步再继续拆成独立 controller/service |

## 当前版本不做事项

| 序号 | 不做项 | 原因 |
| --- | --- | --- |
| 1 | 不在本轮直接删除 `src/biz-content` | 先保留兼容与迁移缓冲层，降低风险 |
| 2 | 不一次性拆成 16 套完全独立模块实现 | 需要更大范围联调和前端同步改造 |

## 验证测试

| 验证项 | 命令 | 结果 | 结论 |
| --- | --- | --- | --- |
| 后端编译验证 | `npm run build` | 通过 | 新的 `StudentBusinessModule` 已可正常注册并编译 |

## 风险与后续计划

| 类型 | 内容 | 优先级 |
| --- | --- | --- |
| 当前风险 | 新模块当前仍以兼容层方式复用原有分表逻辑 | 中 |
| 后续计划 | 按业务域逐步拆成活动、会员、内容、服务等独立模块 | 高 |
