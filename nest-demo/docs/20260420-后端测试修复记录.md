# 20260420-后端测试修复记录

## 目标

对 `nest-demo` 后端执行编译、启动、接口和浏览器流程验证，记录发现的问题、修复动作与复测结果。

## 记录

| 时间 | 阶段 | 问题 | 处理 | 结果 |
| --- | --- | --- | --- | --- |
| 2026-04-20 | 编译前检查 | 认证模块 JWT `expiresIn` 类型与当前依赖版本不兼容，`npm run build` 失败 | 调整 `auth.module.ts` 与 `auth.service.ts` 的 JWT 配置类型声明 | 待复测 |
| 2026-04-20 | 结构整理 | 运行期与脚本辅助能力仍使用 `utils` 命名，职责边界不清晰 | 已迁移到 `src/common/*` 与 `script/support/*`，并更新引用 | 已完成 |
| 2026-04-20 | 文档清理 | 任务文档仍保留 `biz_content` 与“迁移未执行”等过时描述 | 已更新相关文档表述 | 已完成 |
| 2026-04-20 | 启动验证 | `npm run start` 因 `dist/tsconfig.build.tsbuildinfo` 文件权限异常失败 | 改用已编译产物 `node dist/main.js` 进行运行态验证 | 处理中 |
| 2026-04-20 | 登录验证 | 数据库中的 `admin` 用户状态为 `1`，不满足登录校验条件 | 待修复默认管理员状态或补充可用测试账号 | 处理中 |
| 2026-04-20 | 前端联调准备 | 新增 Chrome 本地调试启动脚本，统一打开前端与后端地址 | 新增 `student-manage-system/scripts/start-local-chrome.ps1` | 已完成 |
| 2026-04-20 | 登录验证 | `admin` 密码哈希正确，但账号状态为禁用导致 `/auth/login` 返回 401 | 已将 `sys_user.account = admin` 的 `status` 修正为 `0` | 待复测 |
| 2026-04-20 | 登录复测 | `POST /auth/login` 使用 `admin/admin123` 可正常返回 access token 与 refresh token | 已通过接口复测确认登录恢复 | 已完成 |
| 2026-04-20 | 接口路径核对 | 当前后端控制器未挂载全局 `/api` 前缀，`/api/auth/login` 返回 404；前端环境变量实际使用 `http://localhost:8888` | 记录当前前后端约定，避免按 `/api/*` 误测登录接口 | 已确认 |
| 2026-04-20 | 前端入口核对 | `http://localhost:3000` 返回 404，实际可访问登录页为 `/login` | 将 Chrome 启动脚本默认入口调整为 `http://localhost:3000/login` | 已完成 |
| 2026-04-20 | 浏览器联调 | 已通过脚本启动 Chrome 并打开本地登录页 | 新增独立 Chrome profile 与远程调试端口，避免污染日常浏览器环境 | 已完成 |
| 2026-04-20 | 登录稳定性 | 同一秒内重复登录会因 refresh token 重复触发唯一索引冲突，接口偶发 500 | 在 refresh token payload 中加入 `jti` 随机值，连续登录 3 次复测通过 | 已完成 |
| 2026-04-20 | 权限链路 | 管理员已绑定系统权限，但 `sys-permission` 仅识别 `status = 0`，导致系统菜单和用户接口不可用 | 将权限服务兼容 `0/1` 两种历史启用状态，`/sys/menu/user-menus` 复测通过 | 已完成 |
| 2026-04-20 | 用户列表 | `/sys/user` 查询因排序字段使用数据库列名，触发 TypeORM 元数据解析异常并返回 500 | 补强 `FilterBuilder.addOrderBy`，兼容属性名和下划线列名，`/sys/user` 复测通过 | 已完成 |
| 2026-04-20 | 业务列表 | `student-business/activity` 需带 token 与分类参数访问 | 使用登录 token 访问 `/student-business/activity?page=1&limit=10&category=activity`，数据返回正常 | 已完成 |
| 2026-04-20 | 上传能力 | 后端缺少通用文件上传、预览、下载接口，前端也没有统一上传入口 | 新增 `/files/upload`、`/files/preview`、`/files/download`，并在前端业务页接入上传组件 | 已完成 |
| 2026-04-20 | 富文本能力 | 多张业务表存在 HTML 内容，但前端仍使用纯 `textarea` | 前端引入 `react-quill-new` / `quill`，为协会介绍、入会须知、文章、留学资讯、留创顺德、福利规则等接入富文本编辑 | 已完成 |
| 2026-04-20 | 岗位管理 | 前端请求 `/sys/post`，后端控制器挂在 `sys-post`，存在路径不一致风险 | 后端控制器同时兼容 `/sys/post` 与 `/sys-post`，并调整静态路由顺序 | 已完成 |
| 2026-04-20 | 部门与岗位数据 | 当前数据库 `sys_department`、`sys_post` 表均为空，接口可访问但暂无数据 | 已确认缺失现状，待补充真实迁移源数据后执行导入；当前不使用伪造业务数据填充 | 待提供源数据 |
| 2026-04-20 | 成员风采字段 | `lx_user_manager` 当前仅有 `user_name/avater_url/reg_date/mobile/remark` 等基础字段，Bird 文档中也仅能确认这批字段 | 已完成文档与实体核对，现阶段无法从仓库内发现更多可追溯字段来源，待补充原始需求或旧系统字段定义 | 待确认字段源 |
| 2026-04-20 | 后端联调复测 | 使用本地运行中的 `http://127.0.0.1:8888` 复测登录、岗位、部门与上传链路 | `/auth/login`、`/sys/post`、`/sys/department`、`/files/upload`、`/files/preview` 均返回成功 | 已完成 |
| 2026-04-20 | 前端运行态 | `http://127.0.0.1:3000/login` 当前返回 500，阻塞 UI 流程验证 | 已通过 `tsc --noEmit` 确认代码层类型通过；需重启或修复当前前端运行进程后再继续页面级联调 | 待复测 |
| 2026-04-20 | 旧库核对 | `liuxie.sys_user/sys_post/sys_dept` 仍保留历史业务数据，旧成员数据共 54 条，其中 `sys_user` 混入了后台账号 `admin` | 明确将旧库 `sys_user` 视为成员风采迁移源，但迁移时排除 `admin` 账号 | 已确认 |
| 2026-04-20 | 成员风采建模 | 现有 `member-style` 只有通用控制器和服务分支，实体仍停留在简化版 `lx_user_manager`，无法承接旧库字段 | 新增 `lx_member_style` 实体表，并将 `member-style` 业务切换到新表；控制器继续复用 `MemberBusinessController`，服务由 `MemberBusinessService` 改为读取新表 | 已完成 |
| 2026-04-20 | 旧库数据迁移 | 新库缺少岗位、部门、成员风采历史数据 | 新增迁移 `1714000007000-CreateMemberStyleAndImportLegacyData.ts`，成功导入 `sys_post` 24 条、`sys_department` 5 条、`lx_member_style` 51 条 | 已完成 |
| 2026-04-20 | 成员风采接口复测 | 使用当前运行中的后端服务验证 `member-style` 是否已切到新表 | `/student-business/member?page=1&limit=3&category=member-style` 返回关晓君、叶汶杰等旧库迁移数据，并带 `jobTitle/backgroundImage/studySchool/studyCountry` 等新增字段 | 已完成 |

## 当前结论

- 后端新增上传/预览/下载能力已可用，岗位接口路径问题已修复。
- 富文本编辑器已接入前端代码，但当前运行中的前端服务在 `/login` 仍返回 500，页面级验证被阻塞。
- 旧库 `liuxie` 中的岗位、部门、成员风采数据已经迁移到新库，其中成员风采不再使用简化版 `lx_user_manager`，而是使用新的 `lx_member_style`。
