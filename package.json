{
  "name": "student-engineering",
  "private": true,
  "scripts": {
    "build": "npm run build:server && npm run build:web && npm run build:taro",
    "build:server": "npm --prefix student-manager-server run build",
    "build:web": "npm --prefix student-manage-web run build",
    "build:taro": "npm --prefix student-manager-taro run build:weapp",
    "deploy:admin": "powershell -ExecutionPolicy Bypass -File scripts/deploy-admin.ps1"
  }
}
